# ========== APPLICATION ==========
spring.application.name=order-service
server.port=8081

# ========== DATABASE ==========
spring.datasource.url=jdbc:mysql://localhost:3306/order
spring.datasource.username=root
spring.datasource.password=

# ========== JPA ==========
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# ========== AXON (UTILISE AXON SERVER, PAS KAFKA) ==========
axon.axonserver.servers=localhost:8124
axon.axonserver.enabled=true
axon.eventhandling.processors.default.mode=subscribing

# ========== DÉSACTIVE KAFKA ==========
# spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration

# ========== FEIGN CLIENT (pour product-service) ==========
# Assure-toi que product-service tourne sur le bon port
# product.service.url=http://localhost:8080